// import Dashboard from "@/components/Dashboard";
import { RouteType } from "../types/routes.type";

const privateRoutes: RouteType[] = [
  //   { path: "/dashboard", component: Dashboard, exact: true, protected: true },
];

export default privateRoutes;
