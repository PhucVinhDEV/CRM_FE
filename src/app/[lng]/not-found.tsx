import CustomNotFoundPage from "@/components/commons/CustomNotFound";

const NotFoundPage = () => {
  return (
    <div className="flex h-[100vh] w-full items-center justify-center bg-whiteBase">
      <CustomNotFoundPage />
    </div>
  );
};

export default NotFoundPage;
